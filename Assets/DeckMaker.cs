using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DeckMaker : MonoBehaviour
{
    /*
    [SerializeField] Sprite snowSprite;
    [SerializeField] Sprite fireSprite;
    [SerializeField] Sprite waterSprite;

    [SerializeField] Image card0;
    [SerializeField] Image card1;
    [SerializeField] Image card2;
    [SerializeField] Image card3;
    [SerializeField] Image card4;

    [SerializeField] Text card0Up;
    [SerializeField] Text card0Down;


    Image[] cardImages = new Image[5];
    JitsuDeck deck = new JitsuDeck();
    void Start()
    {
        setImageArray();
        setImageSprites();
        string number = "0";
        number += deck.hand[0].attackLevel.ToString();
        card0Up.text = number;
        card0Down.text = number;
    }
    void setImageArray(){
        cardImages[0] = card0;
        cardImages[1] = card1;
        cardImages[2] = card2;
        cardImages[3] = card3;
        cardImages[4] = card4;
    }
    void setImageSprites(){
        for (int i = 0; i < 5; i++){
            switch (deck.hand[i].element)
            {
            case 0:
                cardImages[i].sprite = snowSprite;
                break;
            case 1:
                cardImages[i].sprite = fireSprite;
                break;
            case 2:
                cardImages[i].sprite = waterSprite;
                break;
            }
        }
    }
    */
}
